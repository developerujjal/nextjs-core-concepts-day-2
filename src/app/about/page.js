import React from 'react';

export const metadata = {
    title: {
        absolute: 'About us Mate'
    }
}

const AboutPage = () => {
    return (
        <div>
            this is About page
        </div>
    );
};

export default AboutPage;